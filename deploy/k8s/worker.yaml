apiVersion: apps/v1
kind: Deployment
metadata:
  name: varc-portal-worker
  namespace: varc
  labels:
    app: varc-portal-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: varc-portal-worker
  template:
    metadata:
      labels:
        app: varc-portal-worker
    spec:
      imagePullSecrets:
        - name: ghcr-pull
      containers:
        - name: worker
          image: ghcr.io/varc-vietnam/varc-portal-api:v1.0.48
          imagePullPolicy: Always
          command: ["/app/varc-worker"]
          envFrom:
            - secretRef:
                name: varc-portal-secrets
            - configMapRef:
                name: varc-portal-config
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1024Mi
